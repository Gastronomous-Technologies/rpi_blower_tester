name: Build and Test
on: [push]

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    steps:
    - uses: actions/checkout@v4
    - name: Build Docker Image 
      run: ./deploy/build.sh
    - name: Test
      run: ./deploy/test.sh
